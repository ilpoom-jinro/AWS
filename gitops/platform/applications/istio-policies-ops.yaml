apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-policies-ops
  namespace: argocd
spec:
  project: ops-infra-project
  source:
    repoURL: http://internal-git-http.git.svc.cluster.local:3000/gitadmin/platform.git
    targetRevision: main
    path: istio
  destination:
    # Ops EKS: ArgoCD가 실행 중인 클러스터 (in-cluster)
    server: https://kubernetes.default.svc
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # istio-system은 Istio 설치 시 이미 존재 → 자동 생성 불필요
      - CreateNamespace=false
