apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finops-mas
  namespace: argocd
spec:
  project: default
  source:
    repoURL: http://internal-git-http.git.svc.cluster.local:3000/gitadmin/platform.git
    targetRevision: main
    path: financial-ops-eks/finops-mas
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: finops-mas
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
