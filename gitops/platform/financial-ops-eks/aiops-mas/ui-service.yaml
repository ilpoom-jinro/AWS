apiVersion: v1
kind: Service
metadata:
  name: aiops-ui
  namespace: aiops-mas
  labels:
    app.kubernetes.io/name: aiops-ui
    app.kubernetes.io/part-of: aiops-mas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aiops-ui
  ports:
    - name: http
      port: 80
      targetPort: http
