apiVersion: v1
kind: Service
metadata:
  name: aiops-ui
  namespace: aiops-mas
  labels:
    app: aiops-ui
spec:
  type: ClusterIP
  selector:
    app: aiops-ui
  ports:
    - name: http
      port: 80
      targetPort: http
