apiVersion: apps/v1
kind: Deployment
metadata:
  name: aiops-ui
  namespace: aiops-mas
  labels:
    app.kubernetes.io/name: aiops-ui
    app.kubernetes.io/part-of: aiops-mas
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: aiops-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: aiops-ui
        app.kubernetes.io/part-of: aiops-mas
    spec:
      serviceAccountName: aiops-ui
      containers:
        - name: aiops-ui
          image: aiops-ui-image
          imagePullPolicy: Always
          env:
            - name: ORCHESTRATOR_URL
              value: http://aiops-orchestrator.aiops-mas.svc.cluster.local
            - name: TEMPORAL_UI_URL
              value: https://temporal-ui.teleport.local
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
