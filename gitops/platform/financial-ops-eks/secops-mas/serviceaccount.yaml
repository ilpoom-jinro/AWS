apiVersion: v1
kind: ServiceAccount
metadata:
  name: secops-orchestrator
  namespace: secops-mas
  labels:
    app: secops-orchestrator
  # NOTE: 실제 Bedrock/KB 호출 시 Pod Identity 등록 필요 (vpc/ops/pod-identity.tf 참조).
  #   현재는 trigger가 없어 워크플로우가 안 돌아 Bedrock 미호출 → Pod 기동엔 불필요.
