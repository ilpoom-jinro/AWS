apiVersion: v1
kind: Service
metadata:
  name: secops-ui
  namespace: secops-mas
  labels:
    app.kubernetes.io/name: secops-ui
    app.kubernetes.io/part-of: secops-mas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: secops-ui
  ports:
    - name: http
      port: 80
      targetPort: http
