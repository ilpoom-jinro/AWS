apiVersion: v1
kind: ServiceAccount
metadata:
  name: slack-hitl
  namespace: platform-mas
  # Pod Identity association 없음 — 봇은 AWS API 직접 호출 안 함.
  # Slack 토큰은 ESO가 K8s Secret으로 꽂아주고, 봇은 그 Secret만 env로 읽음.