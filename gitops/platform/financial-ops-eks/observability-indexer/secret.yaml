apiVersion: v1
kind: Secret
metadata:
  name: observability-indexer-db
  namespace: observability
type: Opaque
stringData:
  # TODO: Replace after Aurora PostgreSQL + pgvector is created.
  # Do not commit real production passwords. Use Secrets Manager or a sealed
  # secret mechanism before production use.
  username: CHANGE_ME
  password: CHANGE_ME
