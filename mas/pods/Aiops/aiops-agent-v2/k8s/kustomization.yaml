# kubectl apply -k k8s/ 로 전체 배포
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aiops

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  # - ingress.yaml   # Slack WebHook 수신 시 cert ARN 교체 후 주석 해제
