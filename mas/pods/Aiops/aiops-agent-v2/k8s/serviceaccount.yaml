# [v0.2 수정] IRSA annotation 제거 — Pod Identity 방식은
# aws_eks_pod_identity_association(terraform)이 SA와 Role을 연결하므로
# annotation이 필요 없다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: aiops-agent
  namespace: aiops
  labels:
    app: aiops-agent
