# Presidio 분석 엔진
presidio-analyzer==2.2.362
# spaCy 본체
spacy>=3.8,<3.9
# spaCy import 시 spacy.cli가 click을 요구함 → 명시적으로 설치
# (최신 spaCy가 click을 핵심 의존성에서 분리해 누락되는 케이스 대응)
click>=8.0
boto3
# 한국어 NER 모델 (whl 직접 설치, spacy download CLI 우회)
https://github.com/explosion/spacy-models/releases/download/ko_core_news_md-3.8.0/ko_core_news_md-3.8.0-py3-none-any.whl
